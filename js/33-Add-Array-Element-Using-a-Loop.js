var pie = prompt("Enter Your Name :" ,"");
document.write('Hello' + pie);

var arr = new Array(3);

for(i=0; i<3; i++){
    arr[i] = prompt("Add something to the ARRAY:");
}

document.write(pie[1] + arr[1] + arr[2])